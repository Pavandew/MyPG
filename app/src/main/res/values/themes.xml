<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SearchRoom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SearchRoom" parent="Base.Theme.SearchRoom" />

    <!--    // Splash Screen-->
    <style name="Theme.SearchRoom.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.SearchRoom</item>
    </style>

    // For EditText Outline color
    <style name="Widget.MyTextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/mid_dark_app_color</item>
        <item name="hintTextColor">@color/mid_dark_app_color</item>
    </style>

</resources>