<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageView
            android:id="@+id/saved_img"
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/app_name"
            android:src="@drawable/room_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <TextView
            android:id="@+id/saved_Title_Tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Shiv PG for Boys"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="#111827"
            app:layout_constraintStart_toEndOf="@id/saved_img"
            app:layout_constraintTop_toTopOf="@id/saved_img"
            app:layout_constraintEnd_toStartOf="@id/saved_btnFav" />

        <TextView
            android:id="@+id/saved_location_Tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:text="Nehru Nagar, Bhilai"
            android:textSize="14sp"
            android:textColor="#6B7280"
            app:layout_constraintStart_toEndOf="@id/saved_img"
            app:layout_constraintTop_toBottomOf="@id/saved_Title_Tv"
            app:layout_constraintEnd_toStartOf="@id/saved_btnFav" />

        <TextView
            android:id="@+id/saved_Price_Tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="10dp"
            android:text="₹ 5,500 / month"
            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="#111827"
            app:layout_constraintStart_toEndOf="@id/saved_img"
            app:layout_constraintTop_toBottomOf="@id/saved_location_Tv" />

        <ImageButton
            android:id="@+id/saved_btnFav"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Remove from saved"
            android:src="@drawable/ic_bookmarkheart_24dp"
            app:tint="#E11D48"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
